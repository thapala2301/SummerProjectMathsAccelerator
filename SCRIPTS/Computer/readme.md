# Commands to run on HP Elite Laptop (Vincent)

For camera:
py -3.13 .\camera_mmio_controller.py 

For audio sender:
pip install sounddevice
python3 audio_sender_s1.py 

For imu: 
python3 relay_imu_data.py

Make sure to change the IP adresses of UDP/TCP 

