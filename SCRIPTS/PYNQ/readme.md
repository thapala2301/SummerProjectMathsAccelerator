# Commands to run on PYNQ Z1 

Make sure all files are correctly pathed (see the pwd path /home/xilinx/jupyter_notebooks/Ray_Marcher_Final )

Then in terminal:
python3 ui_base_selector.py

^C to quit. Once in a scene, ^C to go back to UI.

Make sure to change the IP adresses of PYNQ before connecting

